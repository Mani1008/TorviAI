# ── Rust Backend API Keys (NOT prefixed with VITE_ — never exposed to frontend) ─

# OpenRouter — used for all non-NVIDIA models (Llama, Gemma, Mistral, DeepSeek, GPT-4o…)
# Get a free key at https://openrouter.ai/settings/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here
OPENROUTER_MODEL=nvidia/nemotron-3-super-120b-a12b:free

# NVIDIA NIM — used for Llama 4 Scout, Gemma 4, Nemotron, etc.
# Get a free key at https://build.nvidia.com (select a model → Get API Key)
NVIDIA_API_KEY=your_nvidia_api_key_here

# STT Provider: AssemblyAI — primary real-time streaming STT
# Get a key at https://www.assemblyai.com/dashboard/signup
ASSEMBLYAI_API_KEY=your_assemblyai_api_key_here

# Appwrite server-side API secret — used by Rust (usage.rs) to write rate-limit counters.
# NEVER use VITE_ prefix here — this must NOT reach the frontend bundle.
# Create in Appwrite Console: Project → Settings → API Keys → Add API Key (server scope)
APPWRITE_API_SECRET=your_appwrite_server_api_key_here

# ── Frontend Variables (VITE_ prefix — inlined into JS bundle at build time) ───

# Your production landing page and API base URL
VITE_APP_URL=https://yourapp.com
VITE_API_BASE_URL=https://yourapp.com/api
# Set to true ONLY during local development to skip token verification
VITE_SKIP_AUTH_CHECK=false

# ElevenLabs STT (optional)
VITE_ELEVENLABS_API_KEY=your_elevenlabs_api_key_here

# ── Appwrite ─────────────────────────────────────────────────────────────────────
VITE_APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1
VITE_APPWRITE_PROJECT_ID=your_project_id
VITE_APPWRITE_DATABASE_ID=your_database_id

# Collection IDs (from Appwrite Console → Databases → your DB → Collections)
VITE_APPWRITE_COLLECTION_USER_PROFILES=user_profiles
VITE_APPWRITE_COLLECTION_CONVERSATIONS=conversations
VITE_APPWRITE_COLLECTION_SYSTEM_PROMPTS=system_prompts
VITE_APPWRITE_COLLECTION_USER_SETTINGS=user_settings
VITE_APPWRITE_COLLECTION_SCREENSHOTS=screenshot

# user_usage collection — rate-limit counters.
# Document Security: ON. Collection permissions: NONE (users have read-only on their own doc).
# Attributes needed: aiResponsesUsed (integer, max 600), listeningSecondsUsed (integer, max 40000)
VITE_APPWRITE_COLLECTION_USER_USAGE=user_usage

# Storage bucket ID for screenshot uploads (created by npm run appwrite:provision)
VITE_APPWRITE_BUCKET_SCREENSHOTS=screenshots

# Provision a fresh backend after creating an Appwrite project:
#   APPWRITE_ENDPOINT=... APPWRITE_PROJECT_ID=... APPWRITE_API_SECRET=... npm run appwrite:provision

# ── Cloud backend switch ────────────────────────────────────────────────────────
# appwrite (default) | supabase
VITE_BACKEND_PROVIDER=appwrite

# ── Supabase (when VITE_BACKEND_PROVIDER=supabase) ───────────────────────────
# Project Settings → API → Project URL (must resolve in browser — e.g. https://abcdefgh.supabase.co)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
# Project Settings → API → anon / public key
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here
# Rust usage module only — never prefix with VITE_
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# Supabase Auth dashboard setup (required for Google sign-in):
# 1. Authentication → Providers → Google → enable + add Google OAuth client
# 2. Authentication → URL Configuration:
#      Site URL:  http://127.0.0.1:18427/callback
#      Redirect URLs (add ALL of these):
#        http://127.0.0.1:18427/callback
#        http://127.0.0.1:18427/**
#    (Torvi listens on fixed port 18427 for OAuth — do NOT use localhost:1420)
# 3. Google Cloud Console → OAuth client → Authorized redirect URIs:
#        https://<your-project-ref>.supabase.co/auth/v1/callback
# 4. Run supabase/migrations/001_initial_schema.sql in the SQL editor

# Local dev only — skip cloud sign-in (never use in production builds):
# VITE_SKIP_AUTH_CHECK=true

# ── Google Integrations (Gmail / Calendar OAuth — Desktop client) ─────────────
# Separate from Supabase/Appwrite user sign-in. Create in Google Cloud Console:
#   APIs: Gmail API + Google Calendar API
#   Credentials → Create OAuth client ID → Application type: Desktop app
# For local `tauri dev`, put values here. For release .exe packaging, bake them
# at compile time via build.rs / option_env! — a runtime .env will NOT exist on
# user machines.
#
# OAuth uses a one-shot loopback on http://127.0.0.1:<ephemeral>/callback
# (Desktop clients allow this). Binding 127.0.0.1 usually skips Windows Firewall
# prompts; verify on a clean machine during E2E and document if a prompt appears.
GOOGLE_INTEGRATIONS_CLIENT_ID=your_google_desktop_client_id.apps.googleusercontent.com
GOOGLE_INTEGRATIONS_CLIENT_SECRET=your_google_desktop_client_secret
